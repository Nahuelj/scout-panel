// Connection URL is configured in prisma.config.ts (Prisma 7+).
// Models live in prisma/models/* and enums in prisma/enums/*.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
